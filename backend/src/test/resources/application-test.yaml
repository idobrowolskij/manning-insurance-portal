spring:
  datasource:
    url: jdbc:tc:postgresql:14.5-alpine:///
    username: test-pg
    password: test-pg

  liquibase:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true